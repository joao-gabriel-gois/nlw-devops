apiVersion: v1
kind: Secret

metadata:
  name: db-connection
type: Opaque

data:
  url: base64-encoded-digital-ocean-db-conn-url
# Remove .template after changing value above. This project gitignore already prevent you secret.yaml to be pushed.